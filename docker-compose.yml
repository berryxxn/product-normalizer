services:
  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend-react
    ports:
      - "3000:80"
    depends_on:
      - backend
